# DOT Flow Trigger

Triggers apt publishing after updating the DOT map flow visualization layer.

Rebuild: visible DOT flow visualization layer.

Rebuild: reliable circle-based DOT traffic flow dots.
