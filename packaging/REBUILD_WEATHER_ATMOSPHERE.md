# Rebuild Trigger

This file triggers a Debian package rebuild for the atmospheric weather radar layer update.

Change included:
- weather radar layer uses zoom-based transparency
- aircraft and ground infrastructure remain visible above weather
- weather is treated as an atmospheric overlay rather than an opaque map blanket
