# TileSpace Safe MVP

Package marker for the first safe TileSpace MVP checkpoint.

This checkpoint adds registry metadata, safe tile runtime card components, and developer preview documentation. It does not change Dock behavior and does not mount full-screen map pages inside tiles.
