# Restore radar node rendering

Triggers apt package publishing for restored flight-map radar node rendering and direct radar hit testing. The flight map once again renders radar-region points and active radar zones, prioritizes radar clicks before aircraft clicks, and queries radar nodes directly around the cursor.
