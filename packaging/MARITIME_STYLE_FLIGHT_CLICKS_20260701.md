# Maritime-style flight map clicks

Triggers apt package publishing for the flight map click repair modeled after the maritime map. The flight map now routes clicks by the first MapLibre interactive feature layer: radar nodes, airport pins, airbase pins, then aircraft points/click targets.
