# TileSpace Preview Panel

Package marker for the next TileSpace checkpoint.

Adds a safe React preview panel for the Intel Maps quad using embeddable TileRuntimeCard components. Dock behavior is untouched. Existing Desk runtime is untouched. Full-screen map pages are not mounted inside tiles.
