# Clicked aircraft details fallback

Triggers apt package publishing for the flight details fallback repair. Plane clicks now query the aircraft layers near the cursor and create a detail-panel aircraft record directly from the clicked feature when live state lookup misses.
