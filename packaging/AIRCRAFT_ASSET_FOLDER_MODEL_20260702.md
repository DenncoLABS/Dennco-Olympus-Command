# Aircraft asset folder model

Triggers apt package publishing for the aircraft asset model. Each aircraft now resolves as an aircraft asset with an asset ID, database path, folder path, identity details, telemetry data, history, documents, and notes containers. Live flight snapshots attach the aircraft asset to each aircraft state.
