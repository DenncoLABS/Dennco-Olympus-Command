# Flight details panel and crosshair cursor

Triggers apt package publishing for the flight map detail-panel repair. Aircraft clicks now save a local clicked-flight snapshot so the target detail panel opens reliably, and the map cursor is forced to crosshair instead of switching to pointer when a flight is selected.
