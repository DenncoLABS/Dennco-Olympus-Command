# Rollback unstable flight patches

Restores the build-safe state by disabling experimental flight map click bridge and DOM-injected flight data launcher patches. Use this package as a clean baseline before applying a smaller React-only fix.
