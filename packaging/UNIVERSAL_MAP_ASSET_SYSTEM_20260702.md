# Universal map asset system

Triggers apt package publishing for the universal map asset foundation. Planes, vessels, vehicles, and custom map objects now share a common asset record model with unique IDs, folders, details, photo path support, tracking state, telemetry, history, documents, and notes. Aircraft radar contacts are backed by the same asset store so new tracked contacts can become assets automatically.
