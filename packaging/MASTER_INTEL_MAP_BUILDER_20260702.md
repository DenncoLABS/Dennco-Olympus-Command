# Master Intel Map Builder

Triggers apt package publishing for the Master Intel Map builder. The Intel Maps workspace now includes a Master Map entry that opens a builder state for filtering assets, layers, and feeds, then saving that state into a new Intel Map without reintroducing the failed asset backend.
