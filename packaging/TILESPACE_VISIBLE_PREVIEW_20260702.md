# Visible TileSpace Preview

Package marker for restoring the visible TileSpace preview window on Tile Screen 1.

This mounts the safe TileSpace preview panel inside the existing TileScreens workspace. Dock behavior is untouched. Existing arrows and Tile Screen navigation remain in place.
