# Intel map asset movement model

Triggers apt package publishing for movement history and saved Intel map filtering. Assets now persist movement points into their own history/movement.json files. Saved Intel Maps now store assetFilters, trackedAssetIds, and movement display settings so maps behave as saved operational views over shared assets.
