# Nearest flight map target resolver

Triggers apt package publishing for the unified nearest-target click resolver. The flight map now checks nearby aircraft and radar nodes together, compares screen distance from the cursor, and selects the nearest valid target instead of using fixed radar-first or aircraft-first priority.
