# Rebuild Trigger

This file triggers a Debian package rebuild for the maritime node architecture update.

Changes included:
- INTEL dropdown z-index and click behavior fix
- hard-coded maritime ports layer
- hard-coded Coast Guard and Navy installations layer
- clickable maritime feed nodes with active scan rings, pulse, and sweep line
- backend maritime snapshot filtering by active maritime nodes
