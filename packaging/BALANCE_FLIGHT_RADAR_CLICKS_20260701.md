# Balance flight and radar clicks

Triggers apt package publishing for balanced aircraft/radar click priority. Aircraft clicks are checked first again, radar hit testing uses a smaller radius, and radar node rendering remains restored.
