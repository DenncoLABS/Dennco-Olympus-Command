# Scoped Intel map appearance

Triggers apt package publishing for scoped Intel map appearance. MapInstanceProvider now supplies each Intel map window's own map layer, projection, and weather radar settings through React context instead of falling back to the global theme store for every map.
