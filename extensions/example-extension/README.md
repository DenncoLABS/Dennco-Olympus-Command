# Example Extension

This folder is a starter template for Olympus Command extensions.

Use it when adding optional capabilities such as:

- new map layers
- new intelligence feeds
- client dashboard widgets
- server-side source adapters
- background schedulers
- alert processors

Keep the manifest stable and update the README whenever the extension adds new environment variables, API permissions, or service dependencies.
